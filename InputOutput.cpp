
//  IN this time instead of writing multiple Header I will use a header file that includes all.
// we will also use namespace so don't  need to write std:: multiple times.

#include <bits/stdc++.h>
using namespace std;

int main()
{
    cout << "Hey Today we will be learing about talking input and show output \n";
    // Todays code
    int x, y;
    // we can take input using cin
    cout<<"Enter the value of x:\n";
    cin >> x;
    cout<<"Enter the value of y:\n";
    cin >> y;
    cout << "Value of x is :" << x << "\n";
    cout << "Value of y is :" << y << "\n";
    return 0;
}
